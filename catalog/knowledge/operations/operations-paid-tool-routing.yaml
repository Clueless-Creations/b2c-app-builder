schema_version: 1.0.0
id: reference.operations.paid-tool-routing
title: Paid Tool Routing
domain_id: domain.operations
document_path: knowledge/operations/paid-tool-routing.md
load_when: before using or replacing any paid/account-gated tool, before running a free fallback, or when a service is
  missing from the runtime
lifecycle: active
bindings:
  workflow_ids:
    - workflow.operations.paid-tool-routing-and-fallback
    - workflow.engineering.mobai-device-automation-and-demo-videos
    - workflow.growth.paid-user-acquisition-system
    - workflow.growth.fastlane-growth-ops
    - workflow.orchestration.full-launch-program
  context_pack_ids:
    - context.operations
sources:
  - id: experience-card-deck
    name: Experience Card deck (bundled)
    source_type: official_docs
    url: https://github.com/Clueless-Creations/b2c-app-builder/tree/main/knowledge/experience/experience-cards
    review_cadence_days: 30
    claim_scope: The frozen offline contract for Experience Card content - mechanic definitions with risk ceilings,
      versioned card text, and ethics attestation scaffolds. Every validator reads this deck, so it is the source of
      record for every claim here. An optional first-party MCP server can serve the same deck over stdio when one is
      connected; it carries no claim the deck does not already carry, and is tooling rather than a source.
    last_review_date: 2026-08-17
    reviewer: b2c-maintainers
