# #78 bounded sample

Maintainer review of four merged changes and one recorded no-change.
This is not a runtime gate and not a new proposal schema.

Builder pin: current `main` at authoring time. Predicted impact is labeled
predicted. Unknown cost stays unknown.

## Verdict

Existing `problem`, `proposal`, and `enforcement` fields already capture the
rationale #78 asks for. The CONTRIBUTING and adoption-guide paragraphs already
limit that form to default-path cost or a new architectural boundary. Ordinary
fixes and optional citations do not need it.

**No schema expansion.** Do not add questions. Keep optional providers optional.

## Sample

| Change | Problem / requirement already named | Existing owner considered | How benefit was observed | Default, optional, or experimental | What the fields already covered |
| --- | --- | --- | --- | --- | --- |
| #156 workflow response | Hosted warning pointed at a workspace plan the caller does not have | `kernel/knowledge-service/service.ts` | Fixture on live `coverage.delivery` and the reworded warning | Default hosted/local contract | Named failure, least change at the owner, check as enforcement |
| #165 / #171 Stage A pin | Combined DESIGN.md walk could take the Communication Brief | Existing `get` / heading pin, not a new index | Eight-class Stage A golden plus combined `nextCall` walk | Evaluation fixture, not product infra | Binding/heading fix instead of retrieval infrastructure |
| #151 eval baselines | `store-010` is a `candidates` tie, not a unique first rank | `matchWorkflows` stays out of hosted `catalog()` | Replay fixture; hosted page still reaches needed Apple workflows | Local eval only | Existing owner; no parallel scorer or catalog extract |
| #77 overlay order | No measured failure in authored phase `order` | Catalog phase order already transcribed into the overlay | Fixture equality check | Keep current transcription | Graph-style derivation deferred; no named consumer |
| #88 Expo proof matrix | No selected Expo app, device, or matching #72 authority | Existing proof contracts | Protocol freeze only | Optional / blocked | Specialized surface stays available without becoming compulsory |

## Missing information

None of the five rows needed a fifth mandatory question. Predicted extra
template fields would add maintenance without a named failing proposal form.
Unknown live-eval and device cost stay unknown.

## What this does not do

- Does not close #78.
- Does not add an anti-complexity workflow, file-count quota, or policy database.
- Does not demote optional providers or shrink accepted product scope.
