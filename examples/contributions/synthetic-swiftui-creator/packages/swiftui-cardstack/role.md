SYNTHETIC FIXTURE: not a real creator's repository

# Card stack presenter

Present the card stack with the requested count and spacing. Record a rendered
review before reporting a result. A rendered review is a person's or a vision
check's judgment of the capture. It never passes automatically.
