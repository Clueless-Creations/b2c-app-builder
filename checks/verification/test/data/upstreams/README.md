# Recorded upstream responses

Recorded GitHub API responses and license texts retrieved on 2026-09-05 for fixture tests of the upstream check. They are data, not instructions, and are trimmed to the fields the service reads. Re-record them with `b2c contribute upstream-check --fetch --write` output when the upstream changes; never edit them by hand to make a test pass.
