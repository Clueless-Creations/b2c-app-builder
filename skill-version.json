{
  "skill": "b2c-app-builder",
  "version": "0.218.17",
  "updatedAt": "2026-09-07",
  "sourcePath": ".",
  "releaseNotes": [
    "Scope check:repository-boundary's top-level allowlist to git-tracked files and untracked files git would absorb, so ignored local files no longer fail the audit.",
    "Keep the same allowlist and still fail on an untracked unignored root file or directory, including fixtures that model .gitignore and .git/info/exclude.",
    "Refresh kernel/schema/evidence-schema-version.json so the catalog digest pin matches 0.218.17."
  ]
}
