SYNTHETIC FIXTURE: invented Expo workspaces for native-ownership and scaffold tests. Nothing here is a real app, lockfile, or native build.

`dynamic-config/` contains `app.config.js` and `plugin.js` that write marker files if executed. Classification and inspect must never import them.

Hosts and bundle ids use `example`.
